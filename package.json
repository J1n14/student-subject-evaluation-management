{
  "name": "student-subject-evaluation-management",
  "version": "1.0.0",
  "description": "A static HTML/CSS/JS + Bootstrap 5 app backed by Firebase (Authentication + Cloud Firestore, with optional Storage/Hosting). No build step, no server — open the HTML files directly or deploy with Firebase Hosting.",
  "main": "admin-assignments.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
